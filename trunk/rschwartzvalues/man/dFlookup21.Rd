\name{dFlookup21}
\alias{dFlookup21}
\docType{data}
\title{
%%   ~~ data name/kind ... ~~
}
\description{
%%  ~~ A concise (1-5 lines) description of the dataset. ~~
}
\usage{data(dFlookup21)}
\format{
  A data frame with 21 observations on the following 5 variables.
  \describe{
    \item{\code{questionIndex}}{a numeric vector}
    \item{\code{questionName}}{a factor with levels \code{An exciting life } \code{Being curious } \code{Being forgiving } \code{Being honest } \code{Being independent } \code{Being influential} \code{Being loyal } \code{Being moderate } \code{Being obedient } \code{Being successful } \code{Enjoying life } \code{Family security } \code{Having authority } \code{Honouring parents and elders } \code{Pleasure } \code{Protecting the environment } \code{Respect for tradition} \code{Sense of belonging } \code{Social justice} \code{Unity with nature } \code{Wealth }}
    \item{\code{questionExtra}}{a factor with levels \code{(achieving goals)} \code{(avoiding extremes of action and feeling)} \code{(correcting injustice, care for the weak)} \code{(dutiful, meeting obligations)} \code{(enjoying food, sex, leisure etc)} \code{(faithful to my friends, group)} \code{(feeling at one with nature)} \code{(feeling that others care about me)} \code{(genuine, sincere)} \code{(gratification of desires)} \code{(having an impact on people and events)} \code{(interested in everything, exploring)} \code{(material possessions, money)} \code{(preservation of time-honoured customs)} \code{(preserving nature)} \code{(safety for loved ones)} \code{(self-reliant, self-sufficient)} \code{(showing respect)} \code{(stimulating experiences)} \code{(the right to lead or command)} \code{(willing to make allowances for others)}}
    \item{\code{setID}}{a factor with levels \code{ac} \code{be} \code{co} \code{he} \code{po} \code{sd} \code{se} \code{st} \code{tr} \code{un}}
    \item{\code{setName}}{a factor with levels \code{Achievement} \code{Benevolence} \code{Conformity} \code{Hedonism} \code{Power} \code{Security} \code{Self-Direction} \code{Stimulation} \code{Tradition} \code{Universalism}}
  }
}
\details{
%%  ~~ If necessary, more details than the __description__ above ~~
}
\source{
%%  ~~ reference to a publication or URL from which the data were obtained ~~
}
\references{
%%  ~~ possibly secondary sources and usages ~~
}
\examples{
data(dFlookup21)
## maybe str(dFlookup21) ; plot(dFlookup21) ...
}
\keyword{datasets}
